# ============================================================================
# Cloudflare D1 Configuration (Required)
# ============================================================================
CLOUDFLARE_ACCOUNT_ID=your_account_id
CLOUDFLARE_DATABASE_ID=your_database_id
CLOUDFLARE_API_TOKEN=your_api_token_with_edit_permission

# ============================================================================
# Google OAuth Configuration (Optional - for Google login)
# Setup guide: GOOGLE_OAUTH_SETUP.md
# Console: https://console.cloud.google.com/apis/credentials
# 
# Required steps:
# 1. Create project di Google Cloud Console
# 2. Enable Google+ API (atau Google Identity Services)
# 3. Configure OAuth consent screen
# 4. Create OAuth 2.0 Client ID (Web application)
# 5. Add authorized redirect URIs:
#    - http://localhost:5173/auth/google/callback (dev)
#    - https://yourdomain.pages.dev/auth/google/callback (prod)
# ============================================================================
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ============================================================================
# Resend Email Configuration (Optional - for email verification)
# Get from: https://resend.com/api-keys
# ============================================================================
RESEND_API_TOKEN=re_your_api_token
FROM_EMAIL=noreply@yourdomain.com

# ============================================================================
# Cloudflare R2 Configuration (Optional - for file/image uploads)
# Setup guide: R2_SETUP.md
# 
# Steps to get credentials:
# 1. Cloudflare Dashboard > R2 > Create bucket
# 2. R2 > Manage R2 API Tokens > Create API Token
# 3. Copy Access Key ID and Secret Access Key
# 4. Get Account ID from sidebar or Workers & Pages
# 5. (Optional) Enable Public Access in bucket settings for public URL
# 
# Pricing: $0.015/GB/month, FREE egress (bandwidth)
# ============================================================================
R2_ACCOUNT_ID=your_r2_account_id
R2_ACCESS_KEY_ID=your_r2_access_key
R2_SECRET_ACCESS_KEY=your_r2_secret_key
R2_BUCKET_NAME=your_bucket_name
R2_PUBLIC_URL=https://pub-yourid.r2.dev

# ============================================================================
# Node Environment (Development or Production)
# ============================================================================
NODE_ENV=development
